spring.application.name=Make Ur Tasks

# Configuracao do DataSource
spring.datasource.url=jdbc:mysql://localhost:3306/makeUrTasks?createDatabaseIfNotExist=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Configuracao do Hibernate
# ddl = data definition language!
spring.jpa.hibernate.ddl-auto=update

# Configuracao da JPA
# show sql logs
spring.jpa.show-sql=true

api.security.token.secret=${JWT_SECRET:eurealmentenaoseisedeveriaestarcolocandoumasenhataofacil}